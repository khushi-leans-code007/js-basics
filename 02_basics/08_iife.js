// Immediately Invoked Function Expressions

(function chai() {
    console.log(`khushi`);
    // named iife
})();       


// ()() we use iife to avoid global scope pollution like variable  deceleration and also to call a function Immediately

( () => {
    console.log(`khushi`);
    
} )






































































































































